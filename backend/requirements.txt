fastapi==0.112.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
Pillow==10.4.0
ultralytics==8.2.103
requests==2.31.0
pytesseract==0.3.10
python-dotenv==1.0.0

# PyTorch for CPU (EC2 default)
# For CPU-only (EC2 without GPU)
torch==2.1.0
torchvision==0.16.0

# If you have GPU on EC2, replace above with:
# torch --index-url https://download.pytorch.org/whl/cu118
# torchvision --index-url https://download.pytorch.org/whl/cu118
