fastapi==0.112.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
Pillow==10.4.0
ultralytics==8.2.103
requests==2.31.0

# IMPORTANT: Install PyTorch separately for your GPU/CPU.
# For NVIDIA GTX 1650 (Turing), use CUDA 11.x compatible builds.
# See https://pytorch.org/get-started/locally/ for the exact command.
# Example (Windows, CUDA 11.8):
# pip install --index-url https://download.pytorch.org/whl/cu118 torch torchvision torchaudio
