import { StyleSheet } from 'react-native';

export const signupStyles = StyleSheet.create({
  signinContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 20,
  },
});