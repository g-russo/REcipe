import { StyleSheet } from 'react-native';

export const otpVerificationStyles = StyleSheet.create({
  backArrow: {
    fontSize: 24,
    color: '#000',
    fontWeight: 'bold',
  },
  resendContainer: {
    marginTop: 16,
    alignItems: 'center',
  },
});